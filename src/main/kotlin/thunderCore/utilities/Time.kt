package thunderCore.utilities

class Time {
    companion object {
        const val SEC: Long = 20
        const val MIN = SEC * 60
        const val TEN_MIN = MIN * 10
        const val HOUR = MIN * 60
    }
}